# PRD — 지출결의 업무 자동화 웹앱

| 항목 | 내용 |
|---|---|
| 버전 | v1.4 (2026-09-24) — 발급 대장 진행 현황(3단계) 관리 |
| 사용자 | GSI(주) 인사총무팀 담당자 |
| 기술 스택 | Python 3.10+, Streamlit ≥ 1.60, pandas, openpyxl |
| 실행 | `pip install -r requirements.txt` → `streamlit run app.py` |

## 1. 배경 및 목적
지출결의 1건을 처리할 때마다 같은 정보(품목·금액·계좌)를 품의서 하단 문구, 그룹웨어 제목, 완료기안 엑셀, 문자 보고에 **4번 반복 입력**하고 있다.
이 앱은 **한 번 입력하면 4가지 결과물을 자동 생성**해서 반복 입력과 금액 오기를 없앤다.

**성공 기준**
- 1건 처리 시간: 약 10분 → 2분 이내
- 합계·부가세 계산 오류 0건 (모든 금액을 입력 표 하나에서 계산)

## 2. 범위
| 포함 (v1) | 제외 (추후) |
|---|---|
| 기본정보 + 내역 입력, 자동 계산 | 그룹웨어 API 직접 등록 |
| 품의 문구 / 제목 / 문자 생성 + 복사 | 문자 자동 발송 |
| 완료기안 엑셀 생성·다운로드 | 여러 결의서를 한 엑셀에 누적 |
| | 입력 이력 저장(DB) |

## 3. 화면 구성
```
┌ 사이드바: 기본 설정 ──┐ ┌ 메인 ─────────────────────────────────┐
│ 회사 / 사업장 / 부서   │ │ 1️⃣ 입력: 기본정보 12칸 + 내역 표        │
│ 문자 받는 분 / 보내는이│ │    → 계산 결과 표 + 합계 지표 4개        │
│ 수량 단위              │ │ 2️⃣ 품의 문구   │ 3️⃣ 제목 / 5️⃣ 문자     │
└────────────────────────┘ │ 4️⃣ 엑셀 다운로드 버튼                   │
                           └────────────────────────────────────────┘
```

## 4. 기능 요구사항

### F1. 지출결의서 입력
**기본정보**: 작성일, 기안자, 요청자, 문서번호, 결제방법, 증빙구분, 출금회사(GSI/KN), 지급처, 은행, 계좌번호, 예금주, 입금요청일, 긴급건 여부, 부가세 방식, 건명(선택), 사유, 첨부, 특이사항

**내역 표** (`st.data_editor`, 행 추가/삭제 가능)

| 컬럼 | 입력/계산 | 규칙 |
|---|---|---|
| 순번 | 자동 | 품목이 입력된 행만 1, 2, 3… 부여 (빈 행 무시) |
| 품목(사유) | 입력 | 필수 |
| 업체명 | 입력 | 비우면 기본정보의 지급처 사용 |
| 수량 / 단가 / 배송비 | 입력 | 0 이상 숫자, 빈칸은 0 |
| 비고 | 입력 | 선택 |
| 공급가액 / 부가세 / 합계 | 자동 | 아래 계산식 |

**계산식** — 배송비도 부가세 과세 대상 (금액 = 수량 × 단가 + 배송비)

| 부가세 방식 | 공급가액 | 부가세 |
|---|---|---|
| 별도 (기본) | 금액 | ⌊금액 × 0.1⌋ |
| 포함 | round(금액 ÷ 1.1) | 금액 − 공급가액 |
| 면세/영세 | 금액 | 0 |

합계 = 공급가액 + 부가세
> 근거: 기존 지출결의서 샘플의 소계 418,000원(배송비 5,000원 포함) → 부가세 41,800원 → 합계 459,800원.
> 완료기안 샘플도 공급가액 418,000원에 배송비를 포함하고 배송비 칸은 비워 둠.

### F2. 품의서 하단 문구
```
1. 주요내용
  가. 품목: {건명} 건
  나. 수량: {수량}{단위}                 ← 여러 품목이면 "총 N건 (품목 수량, …)"
  다. 금액: 金 {총합계}원 (VAT 포함, 배송비 X원 포함)
  라. 계좌번호: {은행} {계좌번호} (예금주: {예금주})
  ※ 세부내역                              ← 품목이 2개 이상일 때만
    1) {품목} / {업체} / {수량} / {합계}

2. 사유                                    ← 내용이 있을 때만
  가. {사유}

3. 첨부                                    ← 내용이 있을 때만
  가. 견적서 1부.
  나. 통장사본 1부.

4. 특이사항                                ← 내용이 있을 때만
  가. {특이사항}
끝.
```
- **조건부 항목**: 사유·첨부·특이사항은 비어 있거나 `없음`, `-`, `해당없음`, `N/A` 등만 적혀 있으면 **항목 자체를 만들지 않고**, 남은 항목에 1부터 번호를 다시 매김 (예: 사유·특이사항이 없으면 `1. 주요내용` → `2. 첨부` → `끝.`).
- 각 항목 내용은 줄마다 `가.`, `나.` … 번호를 붙임 (이미 번호가 있으면 그대로).
- **건명 규칙**: 직접 입력한 값 우선. 비우면 품목 1개일 때 `{품목}`, 2개 이상일 때 `{첫 품목} 외 N건`. 이미 "건"으로 끝나면 "건"을 덧붙이지 않음.
- 결과는 수정 가능한 Text Area로 표시하고, **[클립보드 복사]** 버튼 제공. 지출결의서 엑셀·HTML 미리보기는 이 문구(수정본)를 항목별로 나눠 넣음 — 번호는 문구에 적힌 번호를 그대로 사용.

### F3. 문서 제목
- 형식: `[{회사}][{사업장}][{부서}] {건명} 건.` → 예: `[GSI][본사][인사총무팀] 명함 제작 건.`
- 수정 가능한 입력칸 + **[제목 복사]** 버튼.

### F4. 완료기안 엑셀 — 원본 템플릿에 값만 채우기
- **템플릿**: app.py 폴더의 `완료기안 결제요청.xlsx` → 없으면 `*완료기안*.xlsx` 중 가장 큰 파일(날짜별 시트가 쌓인 원본). 사이드바에서 경로 변경 가능.
- 원본(시트 57개, 약 2MB)을 한 번 열어 **[입금요청] 시트만 남긴 사본**을 캐시(원본 파일 수정 시각 기준). 원본 파일은 수정하지 않음.
- 헤더 고정 문구(B4 순번, J4 공급가액, AA3 GSI …)로 구조 확인, 합계 행은 `합 계` 문구로 찾음.
- **파일명**: `{작성일 YYMMDD}_완료기안 결제요청.xlsx`

| 셀 | 값 |
|---|---|
| Z3 / AB3 | 출금회사 범례 — 선택된 회사 칸에 `o` |
| 5행~ B | 순번 1, 2, 3… (배경 = 범례의 GSI/KN 색) |
| C · D · G | 문서번호 · 지급처(업체명) · 품목 |
| J · N · Q | 공급가액(배송비 포함) · 부가세 · 배송비(비움 — 이중 합산 방지) |
| R | `=SUM(J:Q)` (원본 수식 형태) |
| V · W · X · Y · Z | 은행 · 계좌번호 · 입금요청일 · 긴급 `o/x` · 비고(비우면 출금회사명) |
| 합계 행 R | `=SUM(R5:U{마지막 데이터 행})`, 인쇄 영역 B2:AD{합계 행} |

- 원본 데이터 칸(5행)보다 품목이 적으면 남는 행을 비우고 **숨김**, 많으면 합계 행을 아래로 옮기고 **원본 5행 서식을 복사**해 행 추가(행별 병합 D:F·G:I·J:M·N:P·R:U·Z:AD 포함).
- 시트를 지운 사본에 원본의 첫 탭 위치(`firstSheet=17`)가 남으면 Excel 인쇄/PDF가 오류 → 0으로 초기화.
- 문서번호가 비어 있으면 경고 문구를 보여주되 다운로드는 허용.

### F5. 문자 보고 템플릿 — 내역 행마다 분리
```
{받는 분}, 안녕하세요.
{보내는 사람 소개}입니다.

결제승인 된 기안 입금 요청건으로 보고드립니다.

1. {업체명1}
- 품목: {품목1}
- 비용: {행1 합계}원

2. {업체명2}
- 품목: {품목2}
- 비용: {행2 합계}원

감사합니다.
```
- 내역 표의 **행 단위**로 번호를 매김 (같은 업체가 여러 행이면 행마다 따로).
- 비용 = 그 행의 합계(공급가액 + 부가세, 배송비 포함) — 완료기안 엑셀의 행별 `합 계`와 같은 금액이며, 모두 더하면 총 입금액.
- 업체명이 비어 있으면 `(업체명 미입력)`으로 표시.
- 받는 분 / 보내는 사람은 사이드바에서 변경 가능 (기본값: 장미선과장님 / GSI(주) 총무팀 사원 김세희)
- 수정 가능한 Text Area + **[문자 복사]** 버튼.

> v1.1: 내역 표 아래의 공급가액/부가세/배송비/총합계 대형 카드는 제거함 (계산 표로 확인).

### F6. 지출결의서 엑셀 — 원본 템플릿에 값만 채우기 (결과 영역 최상단)
- **방식**: `openpyxl.load_workbook()`으로 원본 지출결의서 파일을 불러와 **지정 셀에 값만 덮어쓰고** 저장. 서식·병합·열 너비·데이터 유효성·인쇄 설정·숨김 시트는 원본 그대로.
- **템플릿 위치**: app.py 폴더의 `지출결의서.xlsx` → 없으면 `*지출결의서*.xlsx` (현재 `260821 지출결의서.xlsx`). 사이드바에서 경로 변경 가능. 고정 문구(B3·B10·B62~B68 등)로 구조를 확인하고, 다르면 다운로드 대신 오류 표시.
- **파일명**: `{작성일 YYMMDD}_지출결의서.xlsx`

| 셀 | 값 |
|---|---|
| B5 | `제목 : {문서 제목}` |
| D6 / G6 / J6 | 작성일 / 기안부서 / 사업소명 |
| D7 / G7 / J7 | 결제요청일 / 기안자 / 요청자 |
| I8 | `=I63` (E8의 `NUMBERSTRING` 한글금액은 원본 수식 유지) |
| 11행~ | 품목 수만큼: B `=IF(C="","",ROW()-10)` · C 품목 · F 업체명 · G 수량 · H 단가 · I 배송비 · J `=(G*H)+I` · K 비고 — 서식은 원본 11행 서식 복사 |
| 남는 내역 행 (~61행) | 값 비우고 **행 숨김** (삭제 시 병합·수식이 깨지므로 숨김으로 원본 구조 보존) |
| I62 / I63 | 부가세 / `=SUM(J11:J61,I62)` |
| D64 / G64 | 결제방법 / 증빙구분 |
| E65 / G65 / K65 | 은행 / 계좌번호 / 예금주 (미입력 시 `추후별도기재`) |
| D66 | `  {출금회사}  입금 요청` |
| B70~B77 | 품의 문구 항목(최대 4개)을 제목행 + 본문행으로 순서대로 — 1~3번은 원본 70~75행, 4번째 항목은 원본 3번 서식을 복사한 76~77행(B77:L77 병합 추가). 항목이 적으면 남는 행은 숨김 |
| B78 | `끝.` (원본 '끝.' 서식), 인쇄 영역 A1:M78 |

- 최대 51건(원본 내역 칸 수). 초과하면 결의서를 나누라는 오류 표시.
- 검증: 생성 파일을 원본과 셀 단위 비교 → 채운 셀 외 변경 없음, Excel에서 PDF로 출력해 확인.

### F7. 지출결의서 HTML 미리보기 + [HTML 복사하기]
- F6과 같은 구성을 HTML 표로 렌더링한 iframe 미리보기 (입력한 품목 행만 표시)
- 그룹웨어 편집기에서 `<style>`이 제거돼도 유지되도록 **모든 서식을 인라인 style**로 작성
- **[HTML 복사하기]**: 클립보드에 `text/html`(서식 있는 표)과 `text/plain`(HTML 소스)을 함께 넣음 → 게시판 일반 편집기·HTML 소스 모드 모두 붙여넣기 가능
- 사용자 입력은 모두 HTML 이스케이프 처리

### F8. 발급 대장 (`history.csv` 누적) + 진행 현황 관리
- 화면: `st.tabs` — **[1. 지출결의서 작성]** / **[2. 발급 대장]**
- **자동 기록**: 지출결의서·완료기안 엑셀 다운로드 버튼을 누르면 `on_click` 콜백으로 app.py 폴더의 `history.csv`에 저장 (Excel에서 한글이 깨지지 않도록 UTF-8 BOM, 임시 파일에 쓴 뒤 교체). 환경변수 `JICHUL_HISTORY_PATH`로 위치 변경 가능.
- **같은 건 판정**: `발급ID` = 작성일 + 해시(건명·지급처·합계·기안자, 줄바꿈·연속 공백은 무시). 같은 건을 다시 받으면 새 행을 만들지 않고 갱신 → 지출결의서 발급 후 결재가 끝나 문서번호를 넣고 완료기안을 받으면 **같은 행에 문서번호와 완료기안 발급 시각이 채워짐**. 나중에 문서번호를 비운 채 다시 받아도 기존 문서번호는 유지.
- **컬럼**: 작성일, 문서번호, 기안부서, 기안자, 지급처, 건명, 품목수, 공급가액, 부가세, 합계, 은행, 계좌번호, 예금주, 출금회사, 입금요청일, 긴급, 지출결의서 발급(일시), 완료기안 발급(일시), 승인완료(`O`), 승인일시. 예전 형식 파일은 불러올 때 새 컬럼을 빈 값으로 보충.

**진행 단계** (저장하지 않고 매번 계산 — 문서번호·체크만 바꾸면 자동 반영)

| 단계 | 조건 |
|---|---|
| 🟡 1. 기안작성 중 | 문서번호 없음, 완료기안 미발급 |
| 🔵 2. 상신완료/진행 중 | 문서번호 입력됨 **또는** 완료기안 발급됨 |
| 🟢 3. 최종승인 완료 | 승인완료 체크 (문서번호 유무와 관계없이) |

**발급 대장 탭**
- 최상단 요약 카드: [전체 건수] · [1. 기안작성 중] · [2. 상신완료/진행 중] · [3. 최종승인 완료] 건수 (대장 전체 기준).
- 필터: 진행 단계 [전체 / 기안작성 / 상신완료 / 최종승인완료] + 작성일 범위 · 지급처 · 기안자 검색. 검색 건수·합계 표시.
- 대장 표(`st.data_editor`): **문서번호 입력/수정**, **승인완료 체크박스**만 편집 가능, 나머지는 읽기 전용. 바꾸면 즉시 `history.csv`에 저장하고(승인 체크 시 승인일시 기록, 해제 시 삭제) 표를 새로 그림 — 필터가 걸린 상태에서도 발급ID로 정확한 행에 저장.
- **[전체 발급 대장 엑셀 다운로드]** (`발급대장_YYMMDD.xlsx`, 상태 컬럼 포함, 머리행 고정·자동 필터·금액 서식), 잘못 저장된 건 삭제.
- `history.csv`가 Excel에서 열려 있어 저장하지 못하면 오류/알림으로 안내.

## 5. 공통 동작 규칙
- **수정 내용 유지**: 생성된 문구를 직접 고치면, 그 문구의 원본이 되는 입력값이 바뀌기 전까지 수정 내용을 유지한다. 원본 입력이 바뀌면 다시 자동 생성한다.
- **클립보드 복사**: `navigator.clipboard`를 우선 쓰고, 막혀 있으면 `execCommand('copy')`로 대체한다. 복사 후 버튼에 "✅ 복사 완료!"를 1.8초간 표시한다.
- 품목이 하나도 없으면 결과 영역 대신 안내 문구를 표시한다.

## 6. 비기능 요구사항
- 로컬 PC 또는 사내 서버에서 실행하며 외부로 데이터를 전송하지 않는다.
- 모든 계산은 정수(원) 단위로 한다.
- 입력이 바뀌면 1초 이내에 결과를 다시 계산한다.

## 7. 가정 및 확인 필요 사항
1. 결의서 1건 = 출금회사 1곳, 은행·계좌·입금요청일 1개로 본다 (완료기안의 모든 행에 동일 값).
2. 엑셀 파일명 날짜는 **작성일** 기준이다.
3. 입금요청일/결제요청일은 원본의 `=TODAY()` 대신 입력한 날짜를 넣는다.
4. 지출결의서 원본에 없는 `4. 특이사항`은 요청에 따라 `끝.` 앞에 추가한다.
5. 지출결의서의 내역 행은 원본 11행(입력 행) 서식으로 통일한다. 원본 빈 행에 남아 있던 불규칙한 수식(예: 배송비 칸의 `SUM(G*H)`)은 지운다.
